# 0.2.19

- 修复 0.2.17 起更新下载遇到 GitHub 跳转时被 Electron 取消的问题；改用原生请求事件同步确认跳转，继续逐跳校验 HTTPS 和允许的域名。
- 更新失败按超时、代理、安全连接、磁盘空间和 HTTP 状态提供具体提示；继续使用系统代理，保留下载取消、大小和摘要校验。

# 0.2.18

- 新会话发送区直接显示 Pi 配置中的默认模型，不再统一显示“选择模型”；连接后优先显示 Pi 实际模型，手动选择优先。
- 模型选择器同步标记当前显示的模型；默认名称仅从配置和已有缓存读取，不启动 Pi、不加载扩展、不改写模型设置。
- 进入新会话或重新聚焦窗口时刷新默认配置。未配置具体默认型号时显示“Pi 默认模型”，由 Pi 连接后决定。

# 0.2.17

- 更新检查和下载改用 Electron 网络栈，跟随系统代理；独立临时网络会话不发送浏览器 Cookie 或 Pi 凭据。
- 下载界面区分连接、接收与校验阶段，显示下载大小、实时速度和更细的进度；支持取消并清理未完成文件。
- 连续 45 秒未收到数据时停止并提示重试，继续校验安装包大小、SHA-256、签名与应用身份。

# 0.2.16

- 项目内会话列表展开、收起增加约 220 毫秒的高度与淡入淡出过渡，下方项目同步移动，箭头平滑旋转。
- 兼容不同列表长度与连续切换；收起后会话项不可点击或键盘聚焦，遵循系统减少动态效果设置。

# 0.2.15

- 应用内更新与 Git 安装成功后自动删除旧版，不再长期保存应用备份；替换失败仍可恢复原应用。
- 构建产物移入不参与 Spotlight 索引的目录，发布或 Git 安装成功后清理解包应用，避免系统搜索出现多个 Pi Desk。

# 0.2.14

- 左右侧栏边界支持拖动调宽，宽度保存在本机；双击恢复该侧默认宽度，也可用方向键微调。
- 拖动时直接跟手，展开/收回继续使用原有动画；限制侧栏宽度并为聊天输入区保留空间，窗口缩小时自动适配。

# 0.2.13

- 右侧文件与编辑差异面板增加展开、收回过渡，聊天区宽度同步平滑调整；收回动画结束后再卸载内容。
- 动画时长约 220 毫秒，支持连续快速切换；遵循系统“减少动态效果”设置，关闭中的面板不可交互。

# 0.2.12

- 每条 edit 工具记录增加“查看差异”，在右侧面板可视化展示本次编辑，支持行内/并排视图、行号与红绿增删标记；聊天保持可见，当前编辑入口高亮。
- 使用 Pi 返回并保存在会话中的实际 patch/diff，支持实时完成记录与历史会话；失败或没有保存差异的编辑明确禁用按钮。
- 长差异限制预览行数与字符数并提示截断，完整 Pi 会话记录保留；查看差异不读取当前项目文件或启动 Pi。

# 0.2.11

- 设置中增加应用内更新：检查新版本、显示版本说明与下载进度，下载后重启安装，无需 Git 或 Node.js。
- 按 Apple Silicon/Intel 选择 GitHub Releases 安装包，核对摘要、大小、应用身份、架构与签名。下载失败可重试；替换失败恢复旧版并保留备份。
- 重启前检查进行中的任务、命名、会话编辑与配置加载，不自动结束活动任务；配置和会话保留。
- 设置中的版本号改为读取当前应用版本。
- 增加维护者双架构发布命令，上传草稿并核对 GitHub 摘要后公开发布。

# 0.2.10

- 继续历史会话时取消固定确认弹窗；历史预览栏保留简短说明，点击继续或发送后直接连接原会话。
- 保留应用内同会话连接复用和项目配置授权。没有新增外部终端占用检测，不把通用说明表述为已检测到冲突。

# 0.2.9

- 增加 Git 更新入口：双击更新脚本或运行 `npm run update`，拉取、构建、备份并安装新版；未提交改动和运行中的应用会阻止更新。
- 项目名称只展开或收起会话列表；右侧增加独立的新建会话图标，保留该项目的未发送草稿。
- 项目与置顶会话统一使用“显示更多”，每次增加 5 条，去掉剩余数量和单次展开数量。

# 0.2.8

- 用户消息下增加“编辑并重发”和“从这里分支”；最新消息可在原会话回退后重发，更早消息通过 Pi 原生 fork 另开分支，文字和图片保留。对话编辑不撤销文件。
- 文件差异增加按文件、代码块和整组撤销；已暂存与未暂存分开显示，取消暂存保留文件。确认后重新核对差异，拒绝陈旧预览及无法完整应用的补丁。
- 编辑框内支持扩展确认；取消或失败不自动重发，已经回退的失败会明确提示并保留编辑内容。

# 0.2.7

- 选好项目后提前准备 Pi，首次发送复用准备中的或已就绪的项目连接；模型菜单优先使用项目连接，不再额外启动全局设置连接。
- 准备期间输入区保持可用，并显示后台准备/已就绪状态；需要项目配置授权时先显示明确选择，不静默批准。
- 最多保留一个尚未使用的项目连接，切换项目时替换它；已激活的对话不被回收。历史预览不提前启动 Pi。
- 首次发送先激活连接，再等待就绪、应用模型选择并发送；失败保留草稿，不自动重发。

# 0.2.6

- 模型列表统一以完整模型 ID 为主标题并按 ID A–Z 排序，服务商和友好名称作为说明；避免发现前后显示名不同导致模型跳到其他字母位置。
- 搜索同时匹配完整 ID、服务商与友好名称，忽略大小写、空格及标点分隔，支持“3.7flash”等写法。
- 本次只读调查确认 Pi Desk 缓存中存在 `cpa/antigravity/gemini-3.7-flash-high`；未对上游可调用性作生成验证。

# 0.2.5

- 新会话的模型菜单立即打开，不再等待 Pi 和扩展启动；支持从缓存或最近使用列表中先行选择。
- 本机 Pi 在后台加载完整模型列表，成功后自动更新菜单并保存字段白名单缓存；未缓存时只读近期会话头尾片段取得历史模型 ID，不猜测能力。
- 缓存按 Pi 配置目录、执行路径以及相关配置文件时间隔离；历史与缓存选项明确标识，发送前由项目 Pi 校验。
- 模型菜单加载不锁住输入区，不再显示占据主区域的扩展启动提示。现有项目会话的模型切换仍使用实时 Pi 状态。

# 0.2.4

- 未选项目时，模型和思考设置使用独立的全局配置连接，不再触发文件夹选择。
- 设置连接沿用本机 Pi 和全局扩展，使用临时工作目录、禁止项目资源、禁用工具且不保存会话，不加入项目或会话列表。
- 记住明确选择的模型与思考档位；发送时选择项目，并在发送前应用选择。项目内不可用的设置会报错并保留草稿。
- 只浏览模型列表不覆盖项目的默认模型；普通项目会话与历史续写保持原有流程。

# 0.2.3

- 模型选择按显示名称 A–Z 排序，忽略大小写、数字自然排序；同名按服务商和完整 ID 排序。
- 保留 Pi 返回的思考档位映射，沿用本机自动发现与手动覆盖；明确排除为 null 的档位，并展示不同的上游映射名称。
- 每次打开模型/思考设置重新读取 Pi 的运行时快照；设置后等待新的快照，避免复用修改前的读取结果。
- 思考面板增加重新读取与发现详情入口，继续通过本机网关命令处理上游默认和预算模式；未声明可调档位不再显示为“不支持思考”。
- “重新读取 Pi”读取当前内存状态，不强制联网刷新目录，也不调用模型生成接口。

# 0.2.2

- 在 Markdown 解析阶段接入 CJK 强调扩展，修复中文粗体紧贴引号、括号或标点时可能直接显示星号的问题。
- 使用配套的 GFM 删除线扩展，保持组合格式的解析一致性；明确正文粗体字重。
- 正文和思考内容共用解析设置；历史与流式回复都会使用新规则。代码、转义符号与原始复制内容由正常 Markdown 流程保留，不改写 Pi 会话。
- 未引入浏览器用户脚本或 DOM MutationObserver；不自动补齐流式输出中尚未完成的 Markdown 标记。

# 0.2.1

- 修复发送前模型按钮被禁用、思考强度入口隐藏的问题；点击任一入口即可连接本机 Pi 并设置，加载配置本身不发送模型对话。
- 思考设置区分当前档位、网关上游默认与预算模式；在上游默认模式下选择已支持的档位会正确切换控制方式。
- 模型和思考列表增加加载失败、空列表、无匹配结果和重试提示，设置失败保留弹窗并显示原因。
- 上下文占用改为圆环，点击展开当前 tokens、窗口容量和百分比；每轮回复后更新。修复压缩后 null 用量可能触发格式化错误的问题。

# 0.2.0

- 项目、置顶和搜索结果每组预览 5 个会话，每次展开增加 5 个，支持收起。
- 移除消息中重复的 Pi 头像与署名，缩小消息、工具、输入框和问答卡片的留白。
- Bash 等工具调用默认显示一行，附命令或文件摘要；参数和输出按需展开。
- 完成的对话保留最终答复，前序思考、工具调用及中间消息自动收进工作过程栏。用时根据消息起止时间估算；错误、中断与未完成输出保持可见。
- 设置中增加独立的会话命名模型。开启后，新对话完成首轮时自动命名；也可批量补齐未命名历史记录，支持取消与恢复。
- 支持直接修改历史会话标题，名称存于 Pi Desk 独立元数据，不改写 Pi 原会话。已有明确名称和用户手动名称优先。
- 历史索引识别位于长会话中部的原有会话名称。

本版执行类型检查、生产构建、打包和安装完整性检查；未进行浏览器交互、手工功能或验收测试。历史命名是按用户授权执行的实际操作。
